# PiCar Pro v1 — Python Dependencies
# Install: pip3 install -r requirements.txt
# On Debian 12+: pip3 install --break-system-packages -r requirements.txt

# I2C / Servo
adafruit-circuitpython-pca9685
adafruit-circuitpython-motor
adafruit-circuitpython-busdevice

# OLED Display
luma.oled

# WS2812 LED Strip
rpi_ws281x
spidev

# Web Server
flask
flask_cors
websockets

# Vision / Video
numpy
pillow

# IMU Sensor (optional — also uses smbus from apt package python3-smbus)
mpu6050-raspberrypi

# DS4 Bluetooth Controller (optional — for PS4 gamepad support)
evdev

# GPIO (usually pre-installed on Raspberry Pi OS)
# RPi.GPIO
# gpiozero
