# PiCar Pro — Python Dependencies
# Install: pip3 install -r requirements.txt
# On Debian 12+: pip3 install --break-system-packages -r requirements.txt

# I2C / Motor / Servo
adafruit-circuitpython-pca9685
adafruit-circuitpython-motor
adafruit-circuitpython-busdevice

# OLED Display
luma.oled

# WS2812 LED Strip (primary: rpi_ws281x DMA/PWM; fallback: spidev)
rpi_ws281x
spidev

# Web Server
flask
flask_cors
websockets

# Vision / Video
numpy
psutil
pillow

# IMU Sensor (optional — MPU6050 hardware driver uses smbus from apt)
mpu6050-raspberrypi
